export const metadata = {
    title: 'Explore Curated SaaS | LaunchXact Marketplace',
    description: 'Discover manually curated SaaS products that solve real business problems. Filter by category and find high-value software for your enterprise.',
};

export default function ExploreLayout({ children }) {
    return <>{children}</>;
}
