export const metadata = {
    title: 'Account Login | LaunchXact',
    robots: {
        index: false,
        follow: false,
        googleBot: {
            index: false,
            follow: false,
        },
    },
};

export default function LoginLayout({ children }) {
    return <>{children}</>;
}
